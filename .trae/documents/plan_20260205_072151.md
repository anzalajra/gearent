I will implement a configurable Document Layout system in the Admin panel as requested.

### **1. Backend & Admin Interface**
*   **Create `DocumentLayoutSettings` Page**:
    *   **Location**: Group 'Setting', Sort Order 3 (appearing after 'Product Setup').
    *   **Fields**:
        *   **Branding**:
            *   **Logo**: Image upload (with toggle to show/hide).
            *   **Font**: Select dropdown (Helvetica, Arial, Times New Roman, DejaVu Sans).
            *   **Primary Color**: Color picker (for headers, borders).
            *   **Secondary Color**: Color picker (for backgrounds, accents).
        *   **Company Information**:
            *   Company Name, Address, Phone, Email, Website, Tax ID (TextInput/Textarea).
        *   **Document Content**:
            *   **Header Text**: RichEditor for custom header content.
            *   **Footer Text**: RichEditor for custom footer content.
            *   **Bank Details**: RichEditor/Textarea for payment info.
        *   **Table Options**:
            *   **Striped Rows**: Toggle switch.
            *   **Bordered Table**: Toggle switch.
    *   **Storage**: Save values to the `settings` table via the `Setting` model (keys prefixed with `doc_`).

### **2. PDF Layout Engine**
*   **Create Master Layout (`resources/views/pdf/layout.blade.php`)**:
    *   A single, flexible Blade layout.
    *   **Dynamic Styling**: Uses CSS variables injected from settings for colors and fonts.
    *   **Structure**:
        *   **Header**: Dynamically renders Logo (if enabled) and Company Info.
        *   **Content**: `@yield('content')` container.
        *   **Footer**: Renders Footer Text and Page Numbers.
*   **View Composer**: Register a composer in `AppServiceProvider` to inject `doc_` settings into all `pdf.*` views automatically.

### **3. Refactor Existing PDF Documents**
*   Update the following files to extend the new `pdf.layout`:
    *   `resources/views/pdf/invoice.blade.php`
    *   `resources/views/pdf/quotation.blade.php`
    *   `resources/views/pdf/delivery-note.blade.php`
    *   `resources/views/pdf/checklist-form.blade.php`
*   Replace hardcoded styles and company details with the new dynamic system.

### **4. Verification**
*   Verify the new settings page works and saves data.
*   Check that PDF generation reflects the changes (colors, fonts, content).
